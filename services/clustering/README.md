# Clustering Service

Responsibilities:

- Periodic or event-driven clustering of document embeddings
- Cluster naming & summarization (LLM)
- APIs to view/merge/split clusters

Implementation notes: consider HDBSCAN for variable-density clusters; use LLM for human-friendly labels.
