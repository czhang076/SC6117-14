# Tests

Add unit and integration tests here. Suggested structure:

- `tests/backend/` — API tests for backend
- `tests/services/` — unit tests for clustering/search/ingestion
- `tests/integration/` — end-to-end tests using a test dataset
