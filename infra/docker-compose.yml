version: '3.8'
services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app
  # vector-db placeholder (e.g., qdrant)
  # qdrant:
  #   image: qdrant/qdrant
  #   ports:
  #     - "6333:6333"
  # frontend placeholder
  frontend:
    build: ./frontend
    ports:
      - "3000:3000"
    volumes:
      - ./frontend:/app
